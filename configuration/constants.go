package configuration

const (
	// Version specifies the current Moonfolio version
	Version string = "v0.01"

	// DefaultMinUpdateInterval is the minimum seconds to set the update interval to
	DefaultMinUpdateInterval int = 10
)
